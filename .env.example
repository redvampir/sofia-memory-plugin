# Пример .env: служит только образцом и не должен содержать настоящих данных
# Укажите персональный токен GitHub (PAT)
GITHUB_TOKEN=your_github_token_here
# (опционально) URL репозитория, если он отличается от значения в config.json
GITHUB_REPO=https://github.com/owner/repo.git
# ОБЯЗАТЕЛЬНО: длинный случайный секрет для локального шифрования токенов
TOKEN_SECRET=your_long_random_secret_here
MEMORY_MODE=github
# Базовый URL сервиса для генерации openapi.yaml (используется скриптом build:openapi)
PUBLIC_BASE_URL=http://localhost:10000
