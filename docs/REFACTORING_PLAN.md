# План рефакторинга Sofia Memory Plugin

**Дата:** 2025-11-20
**Цель:** Привести проект к профессиональным стандартам разработки

---

## 📊 Текущее состояние (Аудит)

### 1. Naming Convention - Критическая проблема

**Обнаружено:**
- ✅ **camelCase**: `writeFileSafe`, `updateIndexEntry`, `getContextSnapshot`
- ❌ **snake_case**: `log_restore_action`, `get_context_for_user`, `token_store`
- ❌ **UPPER_SNAKE_CASE**: переменные окружения (это ОК)
- 🚨 **Кириллица_с_подчеркиваниями**: упомянуто в README как "И_Мой_Стиль"

**Проблема:** В JavaScript-экосистеме стандартом является **camelCase**. Смешение стилей создаёт путаницу.

**Решение:** Стандартизировать на **camelCase** для всех функций, переменных и классов.

**Примеры замен:**
```javascript
// Было:
function log_restore_action(user_id, success) { }
const { token_store } = require('./tools/token_store');

// Будет:
function logRestoreAction(userId, success) { }
const { tokenStore } = require('./tools/tokenStore');
```

---

### 2. API Routes - Дублирование и легаси

**Обнаружено в `index.js`:**

| Легаси роут | Канонический роут | Статус |
|-------------|-------------------|--------|
| `/save` | `/api/files/save` | Дубликат |
| `/saveMemory` | `/api/memory/save` | Дубликат |
| `/readMemory` | `/api/memory/read` | Дубликат |
| `/setMemoryRepo` | `/api/system/switch_repo` | Дубликат |
| `/saveLessonPlan` | `/api/lessons/save-plan` | Дубликат |
| `/switch_memory_repo` | `/api/system/switch_repo` | Дубликат |

**Проблема:**
- Обратная совместимость раздувает API
- Неясно, какой эндпоинт использовать
- Сложность поддержки

**Решение:**
1. Оставить только `/api/*` эндпоинты
2. Добавить deprecation warnings для старых роутов
3. Создать `API_MIGRATION.md` для пользователей
4. Удалить легаси роуты в версии 5.0.0

---

### 3. Безопасность токенов

**Текущая реализация:**
```javascript
// tools/token_store.js
// Токены хранятся в: tools/.cache/tokens/<userId>.txt
// Шифрование: AES-256-CBC с TOKEN_SECRET
```

**Проблемы:**
- ❌ Файловая система - нет атомарности операций
- ❌ Нет ротации токенов
- ❌ Логирование токенов зависит от дисциплины разработчика
- ⚠️ Race conditions при параллельных запросах

**Решение:**
1. Добавить мьютексы для операций с файлами токенов
2. Создать автоматический redactor для логов (заменять токены на `***`)
3. Добавить API для ротации токенов
4. Рассмотреть миграцию на SQLite в будущем

---

### 4. Система индексации

**Текущая реализация:**
- `index.json` как массив - для маленьких проектов
- `multi_index/` - для больших проектов (тысячи файлов)

**Проблемы:**
- Линейный поиск по массиву O(n)
- Нет кэширования результатов
- Валидация при каждой загрузке

**Решение:**
1. Добавить LRU-кэш для часто используемых записей
2. Создать вспомогательные индексы (по тегам, типам, приоритетам)
3. Оптимизировать валидацию через debouncing

---

### 5. Mirror Neurons - Недокументированная фича

**Обнаружено:**
- ✅ Код существует и работает (`src/generator/neurons/`)
- ✅ Реализации: `StyleMirrorNeuron`, `EmotionMirrorNeuron`, `StructureMirrorNeuron`
- ❌ Документация только описывает интерфейс, но не алгоритмы

**Пример кода `StyleMirrorNeuron`:**
```javascript
analyze(input) {
  // Анализирует:
  // 1. Среднюю длину предложений
  // 2. Частоту пунктуации
  // 3. Формальность (по наличию сокращений)
  const sentences = input.split(/[.!?]+/).filter(Boolean);
  const avgSentenceLength = totalWords / sentences.length;
  // ...
}
```

**Решение:**
1. Обновить `docs/mirror-neurons.md` с примерами кода
2. Добавить диаграмму работы нейронов
3. Создать туториал по созданию custom neurons

---

## 🎯 План действий

### Фаза 1: Стандартизация (1-2 дня)

- [ ] **1.1** Создать скрипт автоматического рефакторинга naming convention
- [ ] **1.2** Обновить все функции/переменные на camelCase
- [ ] **1.3** Обновить README.md (удалить упоминание "И_Мой_Стиль")
- [ ] **1.4** Запустить тесты и исправить ошибки

### Фаза 2: Консолидация API (1 день)

- [ ] **2.1** Создать middleware для deprecation warnings
- [ ] **2.2** Добавить заголовок `X-Deprecated-Endpoint: true` к старым роутам
- [ ] **2.3** Создать `docs/API_MIGRATION.md`
- [ ] **2.4** Обновить `openapi.yaml` сометками deprecated

### Фаза 3: Улучшение безопасности (1 день)

- [ ] **3.1** Реализовать log redactor для автоматической замены токенов
- [ ] **3.2** Добавить мьютексы для операций с файлами
- [ ] **3.3** Создать endpoint для ротации токенов
- [ ] **3.4** Добавить тесты безопасности

### Фаза 4: Оптимизация (1 день)

- [ ] **4.1** Реализовать LRU-кэш для индекса
- [ ] **4.2** Создать вспомогательные индексы
- [ ] **4.3** Добавить метрики производительности
- [ ] **4.4** Профилирование и оптимизация

### Фаза 5: Документация (1 день)

- [ ] **5.1** Обновить `docs/mirror-neurons.md` с алгоритмами
- [ ] **5.2** Создать туториал по custom neurons
- [ ] **5.3** Добавить архитектурные диаграммы
- [ ] **5.4** Обновить CONTRIBUTING.md

---

## 📈 Метрики успеха

- ✅ 100% кода использует camelCase
- ✅ Все тесты проходят
- ✅ API имеет четкое разделение на актуальные/deprecated
- ✅ Нет токенов в логах
- ✅ Производительность индекса улучшена на 50%+
- ✅ Документация покрывает все основные фичи

---

## 🚨 Риски

1. **Breaking changes** - старые клиенты могут сломаться
   - *Митигация:* Сохранить легаси роуты до версии 5.0.0

2. **Регрессия в тестах** - массовый рефакторинг может сломать функциональность
   - *Митигация:* Запускать тесты после каждого изменения

3. **Время на рефакторинг** - может занять больше времени
   - *Митигация:* Разбить на малые итерации

---

## 💡 Дальнейшие улучшения (после рефакторинга)

1. **TypeScript migration** - добавить типизацию для критичных модулей
2. **SQLite для индекса** - заменить JSON-файлы на БД
3. **Docker-контейнеризация** - упростить деплой
4. **CI/CD pipeline** - автоматизировать тесты и деплой
5. **Мониторинг** - добавить Prometheus метрики
6. **Rate limiting** - защита от злоупотреблений

---

**Автор рефакторинга:** Claude (AI Assistant)
**Утверждение:** Требуется от владельца проекта
